function unpack (file) {

    //Converts the .arcm file (which is a zip file) to .json to be read and 
    //added to the library

}

export async function _import (file) {
    //Importing a Chaimager file

    var unpacked_file = await unpack(file);

    //Adding to library

}